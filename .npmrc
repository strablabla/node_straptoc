# sqlite3 doit être recompilé depuis les sources : le binaire pré-compilé de
# sqlite3>=6 exige GLIBC_2.38, indisponible sur les systèmes plus anciens
# (ex. Ubuntu 20.04 = glibc 2.31). Sans cela, `npm install`/`npm ci` casse
# le chargement du module natif.
build_from_source=true
